# Documentation build dependencies, installed by Read the Docs and by the docs
# CI job. Pinned so a documentation build is reproducible; dependabot proposes
# bumps monthly (see .github/dependabot.yml).
#
# These are also declared as the project's "docs" extra in pyproject.toml.
# myst-parser is required: the algorithm pages are Markdown, and it is not a
# runtime dependency of the package.
sphinx==8.1.3
sphinx-rtd-theme==3.1.0
myst-parser==4.0.1
